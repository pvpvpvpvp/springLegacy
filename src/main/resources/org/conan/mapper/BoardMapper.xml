<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org/DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="org.conan.mapper.BoardMapper">
  	<select id="getBoardList" resultType="org.conan.vo.BoardVO">
  		SELECT * FROM tbi_board
  	</select>
  	<insert id="insert">
  		INSERT INTO TBI_BOARD (BNO, TITLE, CONTENT, WIRITER) 
  		VALUES(board_seq.Nextval, #{title}, #{content}, #{writer})
  	</insert>
  	
  	<insert id="insertSelectKey">
  		<selectKey keyProperty="bno" order="BEFORE" resultType="long">
  			SELECT board_seq.nextval FROM dual
  		</selectKey>
  		INSERT INTO TBI_BOARD (BNO, TITLE, CONTENT, WIRITER) 
  		VALUES(#{bno}, #{title}, #{content}, #{writer})
  	</insert>
  	
  	<select id="read" resultType="BoardVO">
  		SELECT * FROM tbi_board WHERE bno=#{bno}
  	</select>
  	<update id="update">
  		UPDATE tbi_board
  		set title = #{title},
  		content = #{content},
  		updateDate = sysdate
  		WHERE bno = #{bno}
  	</update>
  	<delete id="delete">
  		DELETE from  tbi_board where bno=#{bno}
  	</delete>
</mapper>
